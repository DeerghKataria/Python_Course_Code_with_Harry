a = (2, 4, 5, 3, 2)
a[0] = 45

# Everything in Python is in the form of objects
# So, for some reason if the compiler shows an
# error it would be about the object not being
# supported.

# In this case, the compiler will throw an
# error since the value of tuple cannot be
# changed.